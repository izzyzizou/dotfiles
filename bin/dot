#!/usr/bin/env bash
# dot: install/update dependencies. Run this occasionally to keep your
# environment fresh. Only does real work on macOS right now.

cd "$(dirname "${BASH_SOURCE[0]}")/.."

source script/install
